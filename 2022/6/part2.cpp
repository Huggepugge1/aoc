#include <fstream>
#include <iostream>
#include <string>

int main() {
    int ans = 0;
    std::ifstream infile("input.txt");
    std::string line;
    while (infile >> line) {
        
    }
    
    
    std::cout << ans << std::endl;
}
